{"category": "Changed", "changelog_entry": "Simplified the ignore array in the .jscpd.json file to a single line, improving readability and consistency with the rest of the file.", "summary": "The ignore array in the .jscpd.json file has been reformatted to a single line, improving readability and consistency with the rest of the file. This change does not introduce any breaking behavior, API or CLI changes, or security impact. The config schema has been updated to reflect this change, and users do not need to take any migration steps. This update maintains uniformity in the file's style.", "version": 1}
