{"category": "Added", "changelog_entry": "Enabled explicit control over pre-commit validation by adding `VALIDATE_PRE_COMMIT` to the list of environment variables set to `\"false\"`, making it clear that this step is disabled in the build process with no migration steps required and no change to the API or CLI contract.", "summary": "The linter configuration has been updated to explicitly disable pre-commit validation, making the existing behavior consistent by adding `VALIDATE_PRE_COMMIT` to the list of environment variables set to `\"false\"`. This change affects how the build process handles code validation and requires no migration steps. The API or CLI contract remains unchanged, with no security implications or config schema changes.", "version": 1}
