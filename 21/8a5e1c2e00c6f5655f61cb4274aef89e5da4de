{"category": "Removed", "changelog_entry": "Simplified the GitHub Actions update workflow job to no longer require write access to contents and pull requests, reducing its security exposure and aligning with best practices for workflow security.", "summary": "The GitHub Actions update workflow job no longer requires write access to contents and pull requests, reducing its security exposure. This change is made possible by using a reusable workflow that doesn't need these permissions. The update job now has a smaller security surface, aligning with best practices for workflow security.", "version": 1}
