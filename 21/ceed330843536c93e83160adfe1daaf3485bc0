{"category": "Added", "changelog_entry": "Enforced project-wide coding style consistency by introducing an .editorconfig file that standardizes indentation to 4 spaces and ignores style rules within the .tox directory.", "summary": "The project now enforces consistent coding styles across all files through the addition of an .editorconfig file in the root directory. This configuration applies to shell scripts and the tox environment, setting indentation to 4 spaces and ignoring style rules within the .tox directory. The change aims to improve code readability and maintainability by standardizing formatting conventions project-wide.", "version": 1}
