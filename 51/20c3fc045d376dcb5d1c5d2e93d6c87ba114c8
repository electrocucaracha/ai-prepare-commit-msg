{"category": "Changed", "changelog_entry": "Optimized the map-reduce summarization chain to support mixed diff shapes and variable chunk units, enabling more efficient summarization of oversized git diffs.", "summary": "The map-reduce summarization chain for oversized git diffs has been refactored into a new module, `summarize.py`, with enhanced algorithmic flexibility and clearer separation of concerns. This change allows for mixed diff shapes and avoids quadratic model requests. The pipeline now plans \"chunk\" units that may be whole files, groups of small files, or parts of large diffs, supporting variable diff shapes.", "version": 1}
