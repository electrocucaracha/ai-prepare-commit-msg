{"category": "Changed", "changelog_entry": "Updated Python dependencies to versions 3.14.1 for aiohttp and 4.14.0 for anyio, requiring potential migration steps from users relying on prior package versions.", "summary": "Python dependencies were updated, including aiohttp to version 3.14.1 and anyio to version 4.14.0. This change may require migration steps for users relying on the previous versions of these packages. The update does not appear to introduce breaking behavior or API changes, nor is there a notable security impact.", "version": 1}
