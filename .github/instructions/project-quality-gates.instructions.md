---
applyTo: "**/*"
---

# Project Quality Gates

For any file change in this repository,
run `make lint` to detect linting issues.

Run `make fmt` to apply or validate formatting,
depending on project behavior.

Address issues reported by these commands before finalizing changes,
or report blockers with command output details.
