{"category": "Added", "changelog_entry": "Enabled a map-reduce summarization chain for oversized diffs to automatically shrink them before falling back to the oversized diff warning, improving usability for large diffs and reducing manual intervention.", "summary": "When a diff exceeds the model's context window even after Headroom compression, a map-reduce summarization chain is now used to shrink the diff before falling back to the oversized diff warning. This approach splits the diff into token-bounded chunks, summarizes each chunk, and then combines and reduces the summaries across a bounded number of rounds. The summarization chain improves usability for large diffs and reduces manual intervention. Breaking behavior or migration requirements are not mentioned.", "version": 1}
