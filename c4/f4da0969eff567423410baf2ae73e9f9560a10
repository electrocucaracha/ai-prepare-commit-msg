{"category": "Added", "changelog_entry": "enabled support for custom LiteLLM providers via entry points, allowing integration with corporate gateways or self-hosted servers without modifying the core codebase.", "summary": "The project now supports custom LiteLLM providers, enabling integration with corporate gateways or self-hosted servers without modifying the core codebase. This is achieved through Python entry points in the `ai_prepare_commit_msg.litellm_providers` group. The loader is idempotent and logs warnings for failed plugin loads, ensuring commit message generation is not blocked. Tests cover registration, idempotency, error handling, and the no-provider case.", "version": 1}
