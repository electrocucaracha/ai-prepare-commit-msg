{"category": "Added", "changelog_entry": "Enabled mypy linter environments to ignore missing imports for certain packages during CI, ensuring more accurate type checking and eliminating false-positive errors.", "summary": "Mypy linter environments are now configured to ignore missing imports for certain packages, ensuring more accurate type checking during CI. This change addresses false-positive errors caused by running mypy without project dependencies installed. The updated configuration specifically instructs mypy to ignore missing imports for click, git, headroom, litellm, and pytest packages.", "version": 1}
