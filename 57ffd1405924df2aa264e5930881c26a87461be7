{"category": "Fixed", "changelog_entry": "Resolved sensitive WORKFLOW_TOKEN exposure by securely passing it through environment variables in GitHub Actions workflows.", "summary": "The WORKFLOW_TOKEN secret was updated to be passed securely through the environment in GitHub Actions workflows, addressing potential security risks by limiting token exposure. This change affects the create-pr-action and update.yml workflows, ensuring that sensitive tokens are handled correctly. The update also includes a dependency fix for CVEs in gitpython.", "version": 1}
