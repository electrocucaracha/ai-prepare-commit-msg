{"category": "Changed", "changelog_entry": "Switched the prepare-commit hook to run as a shell command, improving compatibility and reducing dependency on Python in environments where it's not available or desired.", "summary": "The prepare-commit hook's language was changed from Python to system, improving compatibility and reducing dependency on Python in environments where it's not available or desired. This change allows the hook to run as a shell command, but no changes were made to the hook's logic itself. The hook's configuration was updated in the .pre-commit-config.yaml file to reflect the language change.", "version": 1}
