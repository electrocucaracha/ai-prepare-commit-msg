{"category": "Added", "changelog_entry": "Optimized LLM prompts by applying Headroom prompt compression, reducing token usage and improving efficiency, and introduced metrics to track token savings and requests.", "summary": "Headroom prompt compression is now applied to LLM prompts, reducing token usage and improving efficiency. A new CompressionStats dataclass tracks token savings and requests, and a summary is printed to stderr after each run. Prompt compression is bypassed if Headroom is unavailable or fails, ensuring robust fallback behavior. Prompt file validation now raises TypeError instead of ValueError for invalid types.", "version": 1}
