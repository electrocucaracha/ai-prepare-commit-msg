{"category": "Added", "changelog_entry": "Enabled explicit listing of repositories in the .wordlist.txt file structure, requiring no breaking changes or API updates but potentially affecting workflows reliant on the previous organization.", "summary": "The .wordlist.txt file structure has been reorganized to include repositories, which are now explicitly listed. This change adds new entries for \"repos\" and removes none, while rearranging the existing list. The update does not introduce any breaking behavior, API changes, or security implications, but it may require users to adjust their workflows if they were relying on the previous file structure.", "version": 1}
