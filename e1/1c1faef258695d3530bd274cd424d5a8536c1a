{"category": "Changed", "changelog_entry": "Optimized the pre-commit hook to execute in a consistent and isolated Python environment, reducing dependency conflicts and improving reproducibility across different developer setups.", "summary": "The entry in the pre-commit hook configuration has been updated to use \"uv run\" instead of the previous command, ensuring the hook executes in a consistent and isolated Python environment. This change reduces dependency conflicts and improves reproducibility across different developer setups. No changes to hook behavior are expected.", "version": 1}
