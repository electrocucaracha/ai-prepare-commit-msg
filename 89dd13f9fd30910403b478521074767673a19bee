{"category": "Added", "changelog_entry": "Warns developers of oversized diffs that exceed LLM provider limits, skipping unnecessary API calls and providing clear feedback instead of unclear errors or empty messages.", "summary": "Large diffs that exceed the LLM provider's prompt token limits now trigger a clear warning message, skipping unnecessary API calls and improving user feedback. A pre-check estimates prompt size before making the LLM call, and if it exceeds a safe threshold, the function returns a warning message. This change also handles provider rejection of oversized prompts with a similar warning message, avoiding unclear errors or empty commit messages.", "version": 1}
