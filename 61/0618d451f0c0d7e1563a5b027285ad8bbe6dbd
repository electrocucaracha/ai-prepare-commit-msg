{"category": "Changed", "changelog_entry": "Optimized the AI prepare commit message module to improve its usability by enabling debug logging through an optional CLI flag and simplifying prompt message management via YAML files.", "summary": "The AI prepare commit message module was refactored to improve its functionality and usability. The CLI now accepts an optional `verbose` flag for debug logging, reads prompt messages from a YAML file instead of hardcoding them, and uses the `Sequence` type from Python 3.6+. Additionally, non-standard paths are handled by writing the generated commit message directly to the repository's git directory.", "version": 1}
