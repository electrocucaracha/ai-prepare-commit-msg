{"category": "Changed", "changelog_entry": "Stabilized GitHub Action workflow updates by introducing a dedicated function for resolving commit hashes and explicitly handling exceptions and pinned actions.", "summary": "GitHub Action commit hashes are now resolved using a dedicated function, improving reliability and maintainability. The new approach handles exceptions and pinned actions more explicitly, allowing for fine-grained control over which actions are auto-updated. Pinned actions are skipped with clear messages, reducing the risk of breaking workflows due to upstream changes. This change improves the overall robustness of the workflow update process.", "version": 1}
