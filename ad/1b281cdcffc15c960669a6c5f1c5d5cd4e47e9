{"category": "Added", "changelog_entry": "Enabled a timeout for LLM calls in the commit message generation process to prevent model timeouts and allow fallbacks when necessary.", "summary": "A timeout was added to the LLM call in the commit message generation process, allowing for fallbacks when the model takes too long to respond. This change introduces a potential breaking behavior if the default timeout of 10 seconds is not sufficient for users with slow network connections. The API contract remains unchanged, but users must now consider adjusting their environment variables or prompt files to accommodate this new limitation.", "version": 1}
