{"category": "Removed", "changelog_entry": "Simplified CI workflow configuration by removing redundant secrets inheritance from GitHub workflows.", "summary": "The version resolution logic in CI workflows has been improved to better distinguish between SHA and ref types, and redundant secrets inheritance has been removed from GitHub workflows. This change affects how workflows resolve reusable workflow commit hashes and refs, and it also simplifies workflow configuration by removing unnecessary secrets inheritance. The update_versions.sh script has been reformatted for readability.", "version": 1}
