{"category": "Changed", "changelog_entry": "Automated pull request creation and Dockerfile updates now successfully complete due to resolving permission issues caused by insufficient permissions with the switch to WORKFLOW_TOKEN as an environment variable.", "summary": "Automated pull request creation and Dockerfile updates now use WORKFLOW_TOKEN instead of GITHUB_TOKEN to resolve permission issues that caused workflow steps to fail due to insufficient permissions. This change assumes WORKFLOW_TOKEN is properly configured in repository secrets. The update affects the Create pull request for version updates and Dockerfile updater jobs, replacing GITHUB_TOKEN with WORKFLOW_TOKEN as an environment variable.", "version": 1}
